@page
@model GarageManagement.BackOffice.Pages.Admin.Brands.CreateModel

        @{
        ViewData["Title"] = "Create";
        }
        
    <h1 class="mb-4">Ajout d'une Marque</h1>
    <hr />
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-4"></div>
                    <div class="form-group">
                        <span class="text-danger">*</span>
                        <label asp-for="Brand.Label" class="control-label font-weight-bold"></label>
                        <input asp-for="Brand.Label" class="form-control" />
                        <span asp-validation-for="Brand.Label" class="text-danger"></span>
                    </div>
                    
                    <!-- Bouton de soumission -->
                        <br/>
                        <div class="form-group text-center">
                            <input type="submit" value="Ajouter" class="btn btn-primary btn-lg" />
                        </div>
                </form>
                <!-- Lien de retour -->
                <div class="text-center mt-4">
                    <a asp-page="Index" class="btn btn-link">Retour à la liste des marques</a>
                </div>
            </div>
        </div>
    </div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
