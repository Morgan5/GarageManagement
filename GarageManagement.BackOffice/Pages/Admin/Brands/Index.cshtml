@page
@model GarageManagement.BackOffice.Pages.Admin.Brands.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1 class="mb-4">Gestion des Marques</h1>

<p>
    <a asp-page="Create">Ajouter une marque</a>
</p>
<table class="table table-striped table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Brand[0].Label)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Brand) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Label)
            </td>
            <td>
               <a class="btn btn-warning btn-sm" asp-page="./Edit" asp-route-id="@item.Id">Edit</a> <a class="btn btn-info btn-sm" asp-page="./Details" asp-route-id="@item.Id">Details</a>
            </td>
        </tr>
}
    </tbody>
</table>
